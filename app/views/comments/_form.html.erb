<%= form_for [commentable,Comment.new], remote: true do |f| %>

  <div class="form_group">
      <%= f.text_area :body, class: 'form-control', placeholder: t(:enter_comment) %>
  </div>
 <div class="form-group">
    <%= f.submit t(:submit), class: 'btn btn-success' %>
  </div>

<% end %>